import 'package:flutter/material.dart';

class YourArticleScreen extends StatelessWidget {
  const YourArticleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
